@model IEnumerable<HurCoreProj.Service.DTO.NewContentDto>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Haber Listesi</title>
</head>
<body>
<div>
    @foreach (var item in Model)
    {
        <p>@Html.ActionLink(item.Baslik, "Detail","News", new {id = item.Id})</p>
    }
   
</div>
</body>
</html>
